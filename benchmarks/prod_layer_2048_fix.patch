diff --git a/src/pyjuice/layer/kernels/prod.py b/src/pyjuice/layer/kernels/prod.py
index 90693980..e3d19fe6 100644
--- a/src/pyjuice/layer/kernels/prod.py
+++ b/src/pyjuice/layer/kernels/prod.py
@@ -289,7 +289,7 @@ def _forward_backward_kernel_large(node_vals_ptr, element_vals_ptr, local_ids_pt
         mask_edge = (offs_edge < num_edges)
 
         # Re-compute the ch value ids
-        offs_egstart = tl.load(cids_ptr + nblock_id * num_edges + offs_edge) # [BLOCK_N]
+        offs_egstart = tl.load(cids_ptr + nblock_id * num_edges + offs_edge, mask = mask_edge, other = 0) # [BLOCK_N]
         offs_evals = (offs_egstart[:,None] + ntile_id) * batch_size + offs_batch[None,:] # [BLOCK_N, BLOCK_B]
 
     # Accumulate the `node_vals` if required
diff --git a/src/pyjuice/layer/prod_layer.py b/src/pyjuice/layer/prod_layer.py
index fb801670..78c9d494 100644
--- a/src/pyjuice/layer/prod_layer.py
+++ b/src/pyjuice/layer/prod_layer.py
@@ -295,7 +295,7 @@ class ProdLayer(Layer, nn.Module):
                 batch_size = batch_size, 
                 BLOCK_N = BLOCK_N, 
                 BLOCK_B = BLOCK_B, 
-                N_NUM_BLKS = triton.cdiv(num_edges, BLOCK_B), 
+                N_NUM_BLKS = triton.cdiv(num_edges, BLOCK_N), 
                 block_size = block_size, 
                 accum = accum, 
                 partial_eval = partial_eval,
